// Recently added feature that's not yet integrated
export function processData(data: any) {
  console.log('Processing:', data);
}

// New function added recently but not yet used - appears dead but might be WIP
export function newFeature(input: string) {
  return input.toUpperCase();
}

// Old unused function - truly dead
export function legacyProcessor(data: any) {
  // This hasn't been used in months
  return data;
}
